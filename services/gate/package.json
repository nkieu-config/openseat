{
  "name": "@openseat/gate",
  "version": "0.0.1",
  "private": true,
  "description": "Workspace shim so turbo can run the Go waiting room alongside the TypeScript apps",
  "scripts": {
    "dev": "go run .",
    "lint": "go vet ./...",
    "test": "go test -race ./..."
  }
}
