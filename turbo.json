{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "db:generate": {
      "inputs": ["prisma/schema.prisma"],
      "outputs": ["src/generated/**"]
    },
    "build": {
      "dependsOn": ["^build", "db:generate"],
      "env": [
        "API_PROXY_TARGET",
        "NEXT_PUBLIC_API_ORIGIN",
        "NEXT_PUBLIC_GATE_ORIGIN",
        "NEXT_PUBLIC_PAYMOCK_ORIGIN"
      ],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "@openseat/contracts#build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/apps/api/src/schema.gql"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build", "db:generate"]
    },
    "@openseat/web#typecheck": {
      "dependsOn": ["build"]
    },
    "@openseat/contracts#typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/apps/api/src/schema.gql"]
    },
    "test": {
      "dependsOn": ["^build", "db:generate"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
