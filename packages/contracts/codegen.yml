schema: ../../apps/api/src/schema.gql
generates:
  src/graphql.ts:
    plugins:
      - typescript
    config:
      skipTypename: true
      enumsAsTypes: true
      avoidOptionals: true
      maybeValue: T | null
      scalars:
        DateTime: string
        ID: string
